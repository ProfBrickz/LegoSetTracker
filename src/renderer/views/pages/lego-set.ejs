<h1>
	<%= legoSetName %>
		<% if (legoSetCount> 1) { %> x <%= legoSetCount%>
				<% }%>
</h1>

<button onclick="electronAPI.loadPage('lego-sets')">Back</button>

<data-table id="lego-set" hide-on-empty></data-table>
